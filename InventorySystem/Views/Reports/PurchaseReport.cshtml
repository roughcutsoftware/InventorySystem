@model InventorySystem.Web.View_Models.ReportViewModel

@{
    ViewData["Title"] = "Purchase Report";
}

<div class="page-header">
    <h1><i class="fas fa-file-invoice me-2"></i>Purchase Report</h1>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Dashboard</a></li>
            <li class="breadcrumb-item">Reports</li>
            <li class="breadcrumb-item active">Purchase Report</li>
        </ol>
    </nav>
</div>

<!-- Filter Card -->
<div class="card">
    <div class="card-header">
        <i class="fas fa-filter me-2"></i>Filter Purchase Data
    </div>
    <div class="card-body">
        <form asp-action="PurchaseReportData" method="post">
            <div class="row">
                <div class="col-md-4">
                    <div class="mb-3">
                        <label asp-for="DateFrom" class="form-label"></label>
                        <input asp-for="DateFrom" class="form-control" type="date" />
                        <span asp-validation-for="DateFrom" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="mb-3">
                        <label asp-for="DateTo" class="form-label"></label>
                        <input asp-for="DateTo" class="form-control" type="date" />
                        <span asp-validation-for="DateTo" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="mb-3">
                        <label class="form-label">&nbsp;</label>
                        <button type="submit" class="btn btn-primary d-block w-100">
                            <i class="fas fa-search me-2"></i>Generate Report
                        </button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

@if (Model?.Purchases != null && Model.Purchases.Any())
{
    <!-- Report Card -->
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
            <span><i class="fas fa-table me-2"></i>Purchase Report Results</span>
            <form asp-action="ExportPurchaseReport" method="post" class="d-inline">
                <input type="hidden" asp-for="DateFrom" />
                <input type="hidden" asp-for="DateTo" />
                <button type="submit" class="btn btn-danger btn-sm">
                    <i class="fas fa-file-pdf me-2"></i>Export as PDF
                </button>
            </form>
        </div>
        <div class="card-body">
            <!-- Summary Cards -->
            <div class="row mb-4">
                <div class="col-md-4">
                    <div class="card bg-warning text-white">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center">
                                <div>
                                    <h6 class="mb-0">Total Purchases</h6>
                                    <h3 class="mb-0">@Model.Purchases.Count</h3>
                                </div>
                                <i class="fas fa-shopping-cart fa-3x opacity-50"></i>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card bg-danger text-white">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center">
                                <div>
                                    <h6 class="mb-0">Total Cost</h6>
                                    <h3 class="mb-0">@Model.Purchases.Sum(p => p.Subtotal).ToString("C")</h3>
                                </div>
                                <i class="fas fa-dollar-sign fa-3x opacity-50"></i>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card bg-secondary text-white">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center">
                                <div>
                                    <h6 class="mb-0">Total Items Purchased</h6>
                                    <h3 class="mb-0">@Model.Purchases.Sum(p => p.Quantity)</h3>
                                </div>
                                <i class="fas fa-boxes fa-3x opacity-50"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Data Table -->
            <div class="table-responsive">
                <table class="table table-striped table-hover" id="purchaseTable">
                    <thead class="table-dark">
                        <tr>
                            <th>Date</th>
                            <th>Supplier</th>
                            <th>Product</th>
                            <th>Quantity</th>
                            <th>Unit Cost</th>
                            <th>Subtotal</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var purchase in Model.Purchases)
                        {
                            <tr>
                                <td>@purchase.PurchaseDate.ToString("yyyy-MM-dd")</td>
                                <td>@purchase.SupplierName</td>
                                <td>@purchase.ProductName</td>
                                <td>@purchase.Quantity</td>
                                <td>@purchase.UnitCost.ToString("C")</td>
                                <td>@purchase.Subtotal.ToString("C")</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot class="table-dark">
                        <tr>
                            <th colspan="5" class="text-end">Grand Total:</th>
                            <th>@Model.Purchases.Sum(p => p.Subtotal).ToString("C")</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
}
else if (Model?.Purchases != null)
{
    <div class="card">
        <div class="card-body text-center py-5">
            <i class="fas fa-inbox fa-4x text-muted mb-3"></i>
            <h5 class="text-muted">No purchases found for the selected date range</h5>
            <p class="text-muted">Try selecting a different date range</p>
        </div>
    </div>
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        $(document).ready(function() {
            // Initialize DataTable
            $('#purchaseTable').DataTable({
                "pageLength": 25,
                "order": [[0, "desc"]],
                "language": {
                    "search": "Search records:",
                    "lengthMenu": "Show _MENU_ records per page"
                },
                "dom": '<"row"<"col-sm-6"l><"col-sm-6"f>>rtip'
            });

            // Set default dates (last 30 days)
            var today = new Date();
            var lastMonth = new Date();
            lastMonth.setDate(today.getDate() - 30);

            if (!$('#DateFrom').val()) {
                $('#DateFrom').val(lastMonth.toISOString().split('T')[0]);
            }
            if (!$('#DateTo').val()) {
                $('#DateTo').val(today.toISOString().split('T')[0]);
            }
        });
    </script>
}